**上次生成的SQL存在问题，需要修正**：

❌ **错误**：你生成的SQL使用了不存在的列 `{{invalid_table_alias}}.{{invalid_col}}`
📋 **可用列**：表 `{{invalid_table_alias}}` 的可用字段为：{{available_cols}}

✅ **修正要求**：
1. 重新检查SQL中的每个字段
2. **只能使用上述可用列中的字段**，绝对不能使用不在列表中的字段
3. 如果需要的字段不在可用列中，**宁可只返回表中的其他有效字段，也不要猜测**
4. **禁止根据业务逻辑推测字段名**，必须严格使用白名单中的字段

请重新生成SQL：
