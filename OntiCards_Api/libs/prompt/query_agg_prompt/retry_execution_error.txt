**上次生成的SQL执行失败，需要修正**：

❌ **错误信息**：{{error_msg}}

**本次失败 SQL**（请修正这一版本）：
```sql
{{failed_sql}}
```

✅ **修正要求**：
1. 仔细分析错误信息，找出SQL中的问题
2. 检查SQL中的每个字段是否与数据库实际结构匹配
3. 检查字段名、表名是否正确，绝对不能猜测或创造字段名
4. 注意数据类型转换，确保类型兼容
5. 如需类型转换，请使用正确的转换函数（如 CAST、COALESCE、NULLIF 等）
6. 检查语法是否正确
7. **宁可简化查询返回更少字段，也不要生成会执行报错的SQL**
8. 每个 JOIN ON 只能引用该 JOIN 左右输入中已可见的关系别名，以及该子查询依法可见的外层别名；
   禁止引用后续 JOIN 才声明的别名，注意括号 JOIN、逗号 FROM 项、CTE 和子查询的作用域。
9. 保持业务含义、INNER/LEFT/RIGHT/FULL JOIN 类型、关联条件与 ON/WHERE 条件位置；
   不得删除关联条件、改成 CROSS JOIN 或移动外连接过滤条件来规避错误。
10. 重新输出完整的单条只读 SQL，修正后的 SQL 仍必须通过所有表字段白名单及安全校验。

请重新生成正确的SQL：
