你是数据库结构说明的专业补全助手。你的输出必须是严格 JSON，不得包含无关文字、解释或 Markdown。

请为下列数据表补全"缺失的中文说明"，只在缺失时生成；已有注释不得覆盖。

【表信息】
- 表名：{{table_name}}
- Schema：{{schema_name}}
- 现有表描述：{{table_description_or_空}}

【列清单】
{{columns_json_brief}}

【采样数据（可选，如提供则用于增强注释质量）】
{{sampling_data_json}}

【敏感字段信息（可选，如提供则参考此信息确保注释脱敏）】
{{sensitive_fields_json}}

【要求】
1) 若表描述缺失：生成1句**中文表描述**（尽量详细，200字以内，面向业务用途，贴合该表的整体含义）。
   - 必须基于表名、字段信息推断表的真实业务场景
   - 不得使用过于通用的描述（如"数据表"、"信息表"）
   - 应体现表的业务领域和主要用途（如"缺陷管理"、"库存管理"、"订单处理"等）

2) 对 has_comment=false 的列：生成1句**中文字段注释**（≤16字，简洁准确）。
   - **必须符合表的真实业务场景**，不得偏离表的业务语义
   - 充分利用表名、表描述、其他字段信息进行推断
   - 参考字段名的中文拼音、英文单词、业务缩写等进行合理推断
   - 确保字段注释与表整体业务场景保持一致
   - **【重要】如提供采样数据，注释格式应为："业务含义，例如：示例值"**
     * 例如："最后登录时间，例如：2024-01-15 10:30:00"
     * 例如："登录次数，例如：1 ~ 1205 次"
     * 例如："VIP等级，例如：0=非VIP, 1=铜牌, 2=银牌"
     * 例如："用户昵称，例如：张三、李四、王五"
   命名提示：
   - id/uuid → 主键/唯一标识
   - name/title → 名称/标题
   - code → 编码
   - type/category → 类型
   - status/state/flag → 状态
   - num/count/qty → 数量
   - amount/price/cost → 金额/价格/费用
   - rate/ratio → 比率
   - date/time/created_at/updated_at → 日期/时间/创建时间/更新时间
   - desc/remark/notes → 说明/备注
   - password/passwd/secret → 密码(加密)
   - phone/mobile → 手机号
   - id_card/identity/ssn → 身份证号
   - email → 邮箱/电子邮箱
   - balance → 余额
   - address → 地址

   【敏感字段处理规则】
   如果提供了 sensitive_fields 信息，对于敏感字段（如 password、phone、id_card 等）：
   - 注释应体现其"脱敏"或"加密"特性，如"密码(加密)"、"手机号"、"身份证号"
   - 不在注释中暴露数据格式或示例
   - 注释应简洁，不超过16字

   判定原则与优先顺序（必须严格遵守，确保业务场景准确性）：
   - a. **表名和表描述分析**（最重要）：
     * 仔细分析表名（可能是中文拼音、英文单词、业务术语缩写）
     * 结合表描述理解表的整体业务场景和用途
     * 确保字段注释与表名、表描述的业务语义完全一致
     * 示例：表名"sp_pd_defect"（可能是"缺陷"相关），字段注释应围绕缺陷管理业务

   - b. **字段名多维度分析**（关键）：
     * **英文单词分析**：识别完整单词、常见缩写（如id、num、qty、amt、cnt等）
     * **中文拼音推断**：识别拼音首字母缩写（如"缺陷等级"→DEFECT_LEVEL、"地市局"→BUREAU_NAME）
     * **业务术语识别**：识别行业术语、业务缩写（如"消缺"、"归档"、"验收"等）
     * **字段组合推断**：结合多个字段名推断业务含义（如"FIND_TIME"+"FIX_TIME"→发现时间和修复时间）

   - c. **字段类型和属性分析**：
     * 字段类型（INTEGER/VARCHAR/DATE等）提供语义线索
     * 主键/外键属性帮助理解字段角色
     * 默认值可能暗示业务状态或初始值
     * 非空约束可能表示必填业务字段

   - d. **字段间关联分析**（重要）：
     * 分析同一表中其他字段，理解字段间的业务关系
     * 通过字段组合推断业务场景（如"PROVINCE_NAME"+"BUREAU_NAME"→地理层级关系）
     * 识别枚举字段的枚举值模式（如STATE字段可能对应状态枚举）
     * 通过字段命名模式推断（如"CREATE_TIME"+"UPDATE_TIME"→时间戳字段）

   - e. **业务场景一致性检查**（必须）：
     * 确保字段注释与表名、表描述的业务领域一致
     * 不得偏离表的整体业务语义（如缺陷管理表不应出现"商品价格"等无关字段）
     * 不得编造不存在的业务单位或概念
     * 不得使用与业务场景不符的通用描述

   - f. **命名模式匹配**（辅助判断）：
     * 识别常见命名模式：`xxx_id`→关联ID、`xxx_name`→名称、`xxx_code`→编码、`xxx_time`→时间
     * 识别状态字段：`xxx_status`、`xxx_state`、`xxx_flag`→状态/标志
     * 识别数量字段：`xxx_num`、`xxx_count`、`xxx_qty`→数量
     * 识别金额字段：`xxx_amount`、`xxx_price`、`xxx_cost`→金额/价格

   - g. **中文拼音推断技巧**（特别重要）：
     * 识别拼音首字母缩写：如"缺陷"→DEFECT、"地市局"→BUREAU、"省份"→PROVINCE
     * 识别拼音全拼：如"xiaoque"→消缺、"guangdang"→归档
     * 结合业务术语：如"消缺"是电力行业的专业术语，对应"缺陷消除"
     * 注意大小写：全大写可能是缩写（如"BUREAU"），驼峰可能是完整词（如"BureauName"）

   - h. **英文缩写识别**（辅助判断）：
     * 常见业务缩写：MSG→消息、ORG→组织、DEPT→部门、EMP→员工
     * 常见技术缩写：ID→标识、NO→编号、CNT→计数、AMT→金额
     * 状态缩写：FLAG→标志、STATUS→状态、STATE→状态
     * 时间缩写：DT→日期时间、TM→时间、TS→时间戳

   - i. **最终判断**：
     * 综合以上所有信息，生成最符合业务场景的字段注释
     * 如果信息不足或存在矛盾，宁可输出 **null**（宁缺毋滥）
     * 绝对禁止编造、臆测或使用与业务场景不符的描述
3) 不要返回多余字段；不要写 SQL；不要输出 Markdown 或解释性说明。
4) 输出必须是严格的 JSON，键名和结构必须与下方格式完全一致。

【输出格式（严格 JSON）】
{
  "table_description": "<字符串或 null>",
  "columns": [
    {"name": "<列名>", "comment": "<字符串或 null>"},
    ...
  ]
}

【输入示例1：商品表】
表名：products
现有表描述：（空）
列清单：
[
  {"name": "id", "type": "INTEGER", "is_primary": true, "has_comment": false},
  {"name": "name", "type": "TEXT", "is_primary": false, "has_comment": false},
  {"name": "sales_num", "type": "INTEGER", "is_primary": false, "has_comment": false},
  {"name": "price", "type": "INTEGER", "is_primary": false, "has_comment": false}
]

【期望返回示例1】
{
  "table_description": "商品信息表，用于记录商品名称、价格及销量数据",
  "columns": [
    {"name": "id", "comment": "主键ID"},
    {"name": "name", "comment": "商品名称"},
    {"name": "sales_num", "comment": "累计销量"},
    {"name": "price", "comment": "商品价格(元)"}
  ]
}

【输入示例2：缺陷管理表（中文拼音推断）】
表名：sp_pd_defect_03
现有表描述：缺陷管理表
列清单：
[
  {"name": "DEFECT_ID", "type": "VARCHAR", "is_primary": true, "has_comment": false},
  {"name": "DEFECT_LEVEL", "type": "INTEGER", "is_primary": false, "has_comment": false},
  {"name": "BUREAU_NAME", "type": "VARCHAR", "is_primary": false, "has_comment": false},
  {"name": "PROVINCE_NAME", "type": "VARCHAR", "is_primary": false, "has_comment": false},
  {"name": "FIND_TIME", "type": "TIMESTAMP", "is_primary": false, "has_comment": false},
  {"name": "FIX_TIME", "type": "TIMESTAMP", "is_primary": false, "has_comment": false},
  {"name": "STATE", "type": "INTEGER", "is_primary": false, "has_comment": false}
]

【期望返回示例2】
{
  "table_description": "缺陷管理表，用于记录电力设备缺陷的发现、处理、验收和归档全流程信息",
  "columns": [
    {"name": "DEFECT_ID", "comment": "缺陷唯一标识"},
    {"name": "DEFECT_LEVEL", "comment": "缺陷等级(1-4)"},
    {"name": "BUREAU_NAME", "comment": "地市局名称"},
    {"name": "PROVINCE_NAME", "comment": "省份名称"},
    {"name": "FIND_TIME", "comment": "缺陷发现时间"},
    {"name": "FIX_TIME", "comment": "缺陷修复时间"},
    {"name": "STATE", "comment": "缺陷状态(1-7)"}
  ]
}

【推断说明示例2】：
- "DEFECT"是"缺陷"的英文，"LEVEL"是"等级"→"缺陷等级"
- "BUREAU"是"地市局"的拼音首字母缩写，"PROVINCE"是"省份"的英文
- "FIND_TIME"+"FIX_TIME"组合推断为"发现时间"和"修复时间"（缺陷处理流程）
- "STATE"结合表描述"缺陷管理"推断为"缺陷状态"
- 所有字段注释都与"缺陷管理"业务场景一致

【输入示例3：报文表（业务术语识别）】
表名：REMIT_INWARD_MASTER_SWIFT
现有表描述：SWIFT报文主表
列清单：
[
  {"name": "MSG_NO", "type": "VARCHAR", "is_primary": true, "has_comment": false},
  {"name": "IR_MSG_TYPE", "type": "VARCHAR", "is_primary": false, "has_comment": false},
  {"name": "AHIO_FLAG", "type": "VARCHAR", "is_primary": false, "has_comment": false},
  {"name": "CREATE_TIME", "type": "TIMESTAMP", "is_primary": false, "has_comment": false}
]

【期望返回示例3】
{
  "table_description": "SWIFT报文主表，用于记录银行间SWIFT报文的基本信息和流向",
  "columns": [
    {"name": "MSG_NO", "comment": "报文编号"},
    {"name": "IR_MSG_TYPE", "comment": "报文类型代码"},
    {"name": "AHIO_FLAG", "comment": "报文方向标志(I/O)"},
    {"name": "CREATE_TIME", "comment": "报文创建时间"}
  ]
}

【推断说明示例3】：
- "MSG"是"消息/报文"的缩写，"NO"是"编号"的缩写→"报文编号"
- "IR_MSG_TYPE"结合表描述"SWIFT报文"推断为"报文类型"
- "AHIO_FLAG"中"IO"可能表示"Input/Output"，结合"FLAG"推断为"方向标志"
- 所有字段注释都与"SWIFT报文"业务场景一致

【输入示例4：用户表（带采样数据增强）】
表名：t_user
现有表描述：用户信息表
列清单：
[
  {"name": "last_login_time", "type": "TIMESTAMP", "is_primary": false, "has_comment": false},
  {"name": "login_count", "type": "INTEGER", "is_primary": false, "has_comment": false},
  {"name": "vip_level", "type": "SMALLINT", "is_primary": false, "has_comment": false},
  {"name": "nickname", "type": "VARCHAR", "is_primary": false, "has_comment": false}
]
采样数据：
{
  "last_login_time": ["2024-01-15 10:30:00", "2024-03-22 14:20:00", "2024-06-10 09:15:00"],
  "login_count": [1, 5, 12, 25, 1205],
  "vip_level": [0, 1, 2, 3, 4],
  "nickname": ["张三", "李四", "王五", "赵六", "钱七"]
}

【期望返回示例4】
{
  "table_description": "用户信息表，用于记录平台注册用户的基础信息、账户状态与活跃数据",
  "columns": [
    {"name": "last_login_time", "comment": "最后登录时间，例如：2024-01-15 10:30:00"},
    {"name": "login_count", "comment": "登录次数，例如：1 ~ 1205 次"},
    {"name": "vip_level", "comment": "VIP等级，例如：0=非VIP, 1=铜牌, 2=银牌, 3=金牌, 4=钻石"},
    {"name": "nickname", "comment": "用户昵称，例如：张三、李四、王五"}
  ]
}
